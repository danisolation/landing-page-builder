{
  "name": "landing-page-builder",
  "private": true,
  "scripts": {
    "vercel-build": "cd landing-page-fe && npm install && npm run build"
  },
  "dependencies": {
    "@neon/config": "^1.3.1",
    "@neon/env": "^1.2.2"
  }
}
